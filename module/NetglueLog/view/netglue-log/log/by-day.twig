{% extends 'layout/layout1.twig' %}

{% block content %}
<section class="main">
	<div class="container">
		<div class="row">
			<div class="col-lg-12">
				<div class="row">
					<h1>{{ escapeHtml( title ) }}</h1>
				</div>
				<div class="row">
					<div class="btn-group span4 offset4">
						<a class="btn" href="{{ url('netglue_log/by-day', lastWeek) }}">-1 week</a>
						<a class="btn" href="{{ url('netglue_log/by-day', yesterday) }}">-1 day</a>
						<a class="btn btn-primary" href="{{ url('netglue_log/by-day') }}">Today</a>
						<a class="btn" href="{{ url('netglue_log/by-day', tomorrow) }}">+1 day</a>
						<a class="btn" href="{{ url('netglue_log/by-day', nextWeek) }}">+1 week</a>
					</div>
				</div>
			{% include '/netglue-log/log/log-table.twig' with {'records' : records} %}
			</div>
		</div>
	</div>
</section>>


{% endblock content %}